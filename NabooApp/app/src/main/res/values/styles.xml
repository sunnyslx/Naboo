<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="FullScreenStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="CustomRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/takeout_ratingbar_drawable</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">20dp</item>
    </style>
    <style name="MyEditText" parent="Theme.AppCompat.Light">
    <item name="colorControlNormal">@color/editcolor</item>
    <item name="colorControlActivated">@color/editcolor</item>
    </style>

    <!--对话框的样式-->
    <style name="NormalDialogStyle">
        <!--对话框背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="FriendEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">#757575</item>
        <item name="colorControlActivated">@android:color/holo_orange_dark</item>
    </style>

    <!--<style name="MyEditText_1" parent="Theme.AppCompat.Light">-->
        <!--<item name="colorControlNormal">@color/indigo</item>-->
        <!--<item name="colorControlActivated">@color/pink</item>-->
    <!--</style>-->

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">@dimen/address_bainji_textSize</item>
        <item name="actionMenuTextColor">@color/white</item>
    </style>
    <style name="OverflowMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>  <!--设置不覆盖锚点-->
        <item name="android:dropDownVerticalOffset">4dp</item>
        <item name="android:layout_marginRight">4dp</item>
        <item name="android:popupBackground">@color/colorBlue</item> <!--更改背景颜色-->
    </style>

    <style name="ToolbarTheme" parent="AppTheme.AppBarOverlay">
        <item name="actionMenuTextColor">@color/white</item> <!--颜色-->
        <item name="android:textSize">50sp</item> <!--字体大小-->
    </style>

    <!-- pupupwindow的弹出和消失动画 -->
    <style name="popup_window_anim">
        <item name="android:windowEnterAnimation">@anim/friend_in</item>
        <item name="android:windowExitAnimation">@anim/friend_out</item>
    </style>
</resources>
